export interface ICampaign {
  name: string
  category: string
  photoUrl: string
  averageRate: number
  id: string
}

export interface ICampaignData {
  name: string
  category: string
  photoUrl: string
}
